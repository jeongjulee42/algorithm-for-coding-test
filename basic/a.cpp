#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

ll n;

int main(){
	ios_base::sync_with_stdio(0); cin.tie(NULL); cout.tie(NULL);
	cin >> n;
	cout << (n * (n - 1) * (n - 2)) / 6 << '\n' << 3 << '\n';


	return 0;
}