#include<bits/stdc++.h>
using namespace std;

string str;

int main(){
	ios_base::sync_with_stdio(0); cin.tie(NULL); cout.tie(NULL);

	while(getline(cin, str)){
		if(str == "") break;
		cout << str << '\n';
	}
	return 0;
}